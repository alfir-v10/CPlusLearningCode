#include <iostream>
using namespace std;
int main() {
    int a,b,c, a1,b1,c1;
    cin >> a >> b >> c;
    cin >> a1 >> b1 >> c1;
    cout << (a1-a)*3600 + (b1-b)*60 + (c1-c);
  // put your code here
  return 0;
}
